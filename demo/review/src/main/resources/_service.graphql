type Film @extends @key(fields: "id") {
    id: ID! @external
    reviews: [Review!]!
}

"Query root"
type Query {
    reviews(filmId: ID!): [Review!]!
}

type Review @key(fields: "id") {
    comments: [String!]!
    filmId: ID!
    id: ID!
    score: String!
}
