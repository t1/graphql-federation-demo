type Film @key(fields: "id") @extends {
    id: ID! @external
    reviews: [Review!]!
}

"Query root"
type Query {
    reviews(filmId: ID!): [Review!]!
}

type Review @key(fields: "id") {
    comments: [String!]!
    filmId: ID!
    id: ID!
    score: String!
}
